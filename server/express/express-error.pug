extends ../layout/layout

block head-opt
  title Error - #{appName}

block content

  .section.page-title
    h1 Error

  .section.error-report
    if err.code
      | #{'Code: ' + err.code + '\n'}
    if err.stack
      | #{err.stack}
