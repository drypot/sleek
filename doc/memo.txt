db.postThread.renameCollection('threads');
db.post.renameCollection('posts');

db.threads.update(
	{},
	{
		$rename: {
			userName: 'writer',
			cdate: 'created',
			udate: 'updated',
		}
	},
	false, true
);

db.posts.update(
	{},
	{
		$rename: {
			userName: 'writer',
			cdate: 'created',
			fileNameList: 'files',
		}
	},
	false, true
);


upload-file 디렉토리를 upload 로 변경

