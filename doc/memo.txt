db.postThread.update(
	{},
	{
		$rename: {
			userName: 'writer',
			cdate: 'created',
			udate: 'updated',
		}
	},
	false, true
);

db.post.update(
	{},
	{
		$rename: {
			userName: 'writer',
			cdate: 'created',
			fileNameList: 'files',
		}
	},
	false, true
);

upload-file 디렉토리를 upload 로 변경

